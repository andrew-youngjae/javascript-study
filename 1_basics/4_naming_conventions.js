/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더스코어와 달러를 사용할 수 있다.
 * 3) 숫자로는 이름을 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다.
 *  => var const = 'var'; 이런거 안됨
 */

/**
 * Naming Convention 2
 * 
 * 1) camelCase - 대부분의 언어에서 사용 (띄어쓰기 이후 첫번째 글자 대분자로)
 * 2) snake_case - 띄어쓰기 부분에서 언더스코어 - 파이썬 같은 언어에서 사용
 * 3) PascalCase - C# 같은 마이크로소프트 계열의 언어에서 사용함
 */

const anYujin = '안유진';
console.log(anYujin);